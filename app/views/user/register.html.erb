<h1>ユーザー登録画面</h1>
<p>名前を入力してください。</p>
<table>
  <%= form_tag("/user/create", :method => :get) do %>
    <tr>
      <td><%= label :name, "名前：" %></td>
      <td><%= text_field_tag :name %></td>
      <td><%= submit_tag "登録", :comfirm => "この名前で登録しますか？" %></td>
    </tr>
  <% end %>
</table>


<%= link_to 'TOPに戻る', :controller=>'user' %>
